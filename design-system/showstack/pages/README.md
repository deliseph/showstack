# Page-specific overrides

The shared layer lives in `../MASTER.md`. Anything here is a documented
departure from it, per page.

There are none yet. Every page currently uses the shared tokens, base rules
and shell without overriding them — which is the point, and worth keeping.

If you add one, say what the override is, which page needs it, and why the
shared value could not be adjusted instead.
